# ── Dependencies ──
node_modules/
.pnpm-store/

# ── Build output ──
dist/
build/
.output/
.next/
.nuxt/
.vite/

# ── Environment ──
.env
.env.local
.env.*.local

# ── IDE / OS ──
.idea/
.vscode/
.DS_Store
Thumbs.db

# ── Logs ──
*.log
npm-debug.log*

# ── Temp ──
tmp/
temp/
.cache/

# ── Shell ──
.bg-shell/

# ── CloudSync meta ──
.cloudsync_meta.json
.cloudsync_zip_meta.json

# ── Workspace private ──
memory/
.wpscomate/
thumbnail.png
.subagents/

# uniCloud 云函数构建产物（pnpm run build:unicloud 生成）
deploy/unicloud/cloudfunctions/*/nitro/
# 注意：必须锚定根目录（/uniCloud-*/），否则在大小写不敏感的文件系统（Windows）上
# 会误伤 server/data/providers/unicloud-db/ —— 曾导致数据层 7 个文件从未入库
/uniCloud-*/

# ── 构建缓存 ──
*.tsbuildinfo

# ── HBuilderX / uniCloud 部署宿主（账号相关，本地自备，见 deploy/unicloud/README.md）──
/manifest.json
/.hbuilderx/
/unpackage/

*.tmp.mjs

# ── 本地临时文件（不入库）──
.tmp-*
