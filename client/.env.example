# ── 前端构建配置（模板）──────────────────────────────────────────
# 复制为 client/.env 后填值：cp client/.env.example client/.env
# Vite 会自动加载本目录的 .env（client/.env 已 gitignore）。
# 也可在构建时用 shell 变量临时覆盖：VITE_API_BASE=... pnpm run build

# 接口地址（构建时注入产物）。不填则回落到同源相对路径 ./api/…
#
# ⚠️ 开发与生产必须分开，否则本地调试会直打线上云函数：
#   - client/.env                  → 留空。pnpm dev 时走相对路径 ./api/blog，
#                                    由 vite.config.ts 的 /api 代理转到本地 nitro（4917）。
#   - client/.env.production.local → 只在这里填线上地址（.env.*.local 已 gitignore）。
#                                    pnpm build / build:unicloud 才读它。
#
# 历史上 client/.env 直接填了线上地址，绕过了本地代理，导致每次热更新、
# 每张截图、每次刷新都变成对线上云函数的真实调用（两天累计数千次）。
#
# 支付宝云  https://<spaceId>.dev-hz.cloudbasefunction.cn/mz-api
VITE_API_BASE=

# 仅在部署到子目录时需要（如 /mz-corner/）；传到托管根目录就留空
VITE_BASE=
